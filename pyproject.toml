[project]
name = "poc-langgraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.13.1",
    "pytest>=8.4.1",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.1.0",
]
